## Scope

- [ ] Changes are limited to `repos/reality-foundation/**`.
- [ ] No gameplay feature, database, persistence, asset, or binary was added.
- [ ] Fixed versions and source boundaries remain unchanged.

## Verification

List the exact fixed-JDK commands run, including unit tests, Forge compile,
GameTestServer, dedicated-server smoke, artifact/SBOM checks, and whether a
real client screenshot was obtained.
